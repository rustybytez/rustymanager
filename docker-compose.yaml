services:
  app:
    build: .
    expose:
      - "8080"
    environment:
      - AUTH_TOKEN=${AUTH_TOKEN}
      - DATABASE_URL=${DATABASE_URL:-/data/rustymanager.db}
      - PORT=8080
    volumes:
      - db_data:/data

volumes:
  db_data:
