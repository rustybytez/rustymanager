version: "2"
sql:
  - engine: "sqlite"
    schema: "sql/schema/"
    queries: "sql/queries/"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_interface: true
        emit_prepared_queries: false
